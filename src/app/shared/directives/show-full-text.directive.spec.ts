import { ShowFullTextDirective } from './show-full-text.directive';

describe('ShowFullTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowFullTextDirective();
    expect(directive).toBeTruthy();
  });
});
